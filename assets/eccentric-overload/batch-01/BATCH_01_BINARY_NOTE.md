Binary archive source for this batch is staged locally as `eccentric-overload-batch-01-repo.zip` and contains all 10 optimized WebP images listed in this folder's README. The GitHub connector available in-chat does not expose a local-file upload parameter for binary contents, so the manifest is committed here while the binary archive is retained in the conversation workspace for later repository transfer if needed.